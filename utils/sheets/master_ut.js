const generateMasterUtSheet = async (year, sheetId) => {
  console.log(
    `Generating master UT sheet for year ${year} with sheetId ${sheetId}`
  );
};

module.exports = {
  generateMasterUtSheet,
};
